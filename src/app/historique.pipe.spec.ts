import { HistoriquePipe } from './historique.pipe';

describe('HistoriquePipe', () => {
  it('create an instance', () => {
    const pipe = new HistoriquePipe();
    expect(pipe).toBeTruthy();
  });
});
